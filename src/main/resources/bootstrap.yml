spring:
  application:
    name: customer
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      fail-fast: false  # Cambiar a false para desarrollo
      retry:
        max-attempts: 10
        initial-interval: 2000
        max-interval: 3000